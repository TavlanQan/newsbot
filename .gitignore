# Секреты — не коммитить
.env

# SQLite (WAL) — все вариации (db, db-wal, db-shm, db.bak-*, db.old-*, db.recovered)
news_bot.db*

# Логи
logs/
*.log

# Node
node_modules/

# Сессии Telegraf (Задача 1, скоро)
sessions.json

# OS
.DS_Store
Thumbs.db
